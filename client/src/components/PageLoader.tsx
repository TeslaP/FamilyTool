import { HorizonLogoAnimated } from "./HorizonLogoAnimated";

export function PageLoader() {
  return (
    <div className="h-[calc(100vh-5rem)] flex items-center justify-center">
      <HorizonLogoAnimated size={48} mode="both" />
    </div>
  );
}
